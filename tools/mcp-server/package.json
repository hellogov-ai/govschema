{
  "name": "@govschema/mcp-server",
  "version": "0.1.0",
  "description": "Read-only Model Context Protocol server exposing GovSchema registry lookup, search, and validation. Non-normative reference tooling, parallel in spirit to tools/validate.mjs — not part of the GovSchema standard. Plain HTTPS fetch + JSON Schema validation remains fully sufficient without it.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "server.mjs",
  "bin": {
    "govschema-mcp-server": "./server.mjs"
  },
  "files": [
    "server.mjs",
    "README.md"
  ],
  "scripts": {
    "start": "node server.mjs",
    "test": "node test.mjs"
  },
  "dependencies": {
    "@govschema/client": "file:../govschema-client",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.25.0 || ^4.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hellogov-ai/govschema",
    "directory": "tools/mcp-server"
  },
  "publishConfig": {
    "access": "public"
  }
}
