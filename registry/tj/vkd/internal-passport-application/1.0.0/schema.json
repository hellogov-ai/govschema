{
  "$schema": "https://govschema.org/spec/v0.3/govschema.schema.json",
  "govschemaVersion": "0.3.0",
  "id": "tj/vkd/internal-passport-application",
  "version": "1.0.0",
  "title": "Tajikistan — Ариза оид ба додани шиносномаи шаҳрванди Ҷумҳурии Тоҷикистон (Application for Issuance of the Internal Passport of a Citizen of the Republic of Tajikistan)",
  "description": "The Passport Registration Service (Хадамоти шиносномавӣ ва бақайдгирии аҳолӣ, ХШБ) of the Ministry of Internal Affairs' (passportvkd.tj) \"Shakli 1\" (Form 1) — the unified application for issuance of the internal passport (шиноснома), Tajikistan's national-identity-equivalent document, covering first-time issuance, replacement, and renewal in a single form. Opens Tajikistan's National ID & Civic Documents vertical (3 of 6), picked up from Known Gaps entry 0i/0k's own banked candidate (GOV-4424, 2026-07-22; re-delegated by GOV-4488, 2026-07-23), following GOV-4506 (\"GovSchema Standard Research\"). Models the two-sided base application (Shakli 1, numbered items 1-16 and 21-31) plus the numbered continuation sheet Shakli 1/1's own eight-question security/eligibility questionnaire (Саволнома); Shakli 1/1's own restatement of the applicant's already-captured personal data, its system-assigned application-sequence number, its fingerprint-presence indicators, and its payment/office-acceptance blocks are excluded as duplicate, system-generated, or office-only — see this document's own VERIFICATION.md for the full scope record and disclosed findings. This schema does not file the application itself; the live source is always authoritative. GovSchema is independent and is not affiliated with, endorsed by, or operated by the Government of the Republic of Tajikistan, its Ministry of Internal Affairs, or the Passport Registration Service.",
  "status": "draft",
  "jurisdiction": {
    "country": "TJ",
    "level": "national"
  },
  "authority": {
    "name": "Passport Registration Service (Хадамоти шиносномавӣ ва бақайдгирии аҳолӣ) under the Ministry of Internal Affairs of the Republic of Tajikistan",
    "abbreviation": "VKD",
    "url": "https://passportvkd.tj"
  },
  "process": {
    "type": "application",
    "language": "tg"
  },
  "source": {
    "url": "https://passportvkd.tj/wp-content/uploads/2019/11/Zamimaho.docx",
    "retrievedAt": "2026-07-23",
    "documentRef": "Zamimaho.docx — Шакли 1 ва Шакли 1/1 (Form 1 and Form 1/1)"
  },
  "verification": {
    "method": "manual-source-review-v1",
    "lastVerifiedAt": "2026-07-23",
    "verifiedBy": "GovSchema Engineering (Standards Engineer)",
    "nextReviewBy": "2027-01-23",
    "notes": "GOV-4506 (\"GovSchema Standard Research\"). The candidate was found and byte-hashed during the GOV-4424 scouting pass (2026-07-22), which banked Tajikistan's National ID vertical as a ready-to-author 3-of-6 candidate never yet text-extracted; this cycle performs that extraction from scratch rather than trusting the scouting note's own description.\n\n## Reaching the live source\n\nIndependently re-fetched: `https://passportvkd.tj/wp-content/uploads/2019/11/Zamimaho.docx`, HTTP 200, `Content-Length: 608,357` bytes — byte-for-byte matching the GOV-4424 banked record. sha256 `c3f4c20ad42452eda4148a6bb46d3b88322d65fcc94880afa25fdb3c3566beb1`. Genuine Office Open XML (`.docx`, a real ZIP/`[Content_Types].xml` container, not a renamed binary `.doc`). No login/CAPTCHA/WAF gate; a plain `curl` (no `-k` needed, unlike sibling `andoz.tj`) succeeds.\n\n## Extraction method\n\n`passportvkd.tj`'s own \"Zamimaho\" (Appendices) document is a single large docx bundling roughly 35 numbered appendix forms end-to-end (Shakli 1 through Shakli 35+, covering the passport service's entire form library — residence registration, military-obligation deregistration, correction requests, and more), not solely the internal-passport application. `word/document.xml` was extracted directly via Python's standard-library `zipfile` module (no `unzip`/`pip` available in this environment) and each `<w:p>...</w:p>` paragraph's `<w:t>` runs concatenated in document order (with a corrected regex distinguishing literal `<w:t>` from `<w:tab .../>`, whose tag name is a substring-prefix match risk) to reconstruct plain text with paragraph breaks preserved. The scope boundary was located by searching for the form's own `Шакли N` page headers: Shakli 1 begins at the document's second paragraph and Shakli 2 begins 113 paragraphs later, cleanly bounding this schema's own scope to Shakli 1 plus its own numbered continuation sheet Shakli 1/1 (which sits between Shakli 1's own closing office block and Shakli 2's own heading, and is explicitly captioned as belonging to the same application, \"Ариза барои гирифтани шиноснома\" — \"Application for obtaining the passport\"). Confirmed via `getAnnotations()`-equivalent inspection (searching for `<w:sdt>`, `<w:tbl>`, and checkbox/content-control markup within this span) that the form carries zero tables, content controls, or checkbox form-fields — a plain fill-in-the-blank template using underscore rules for every answer, consistent with this registry's prior Bolivia/Zimbabwe/Tajikistan-Taxes non-AcroForm specimens; two `<w:drawing>` elements in this span correspond to the form's own photo-placement boxes (\"РАСМ\" / \"Ҷои сурат\"), not data-bearing content.\n\n## Document structure\n\n**Shakli 1** (two sides of one physical sheet): a front side with 20 numbered items — items 1-14 (applicant's own current and prior name components, sex, date/place of birth, blood group and Rh factor, marital status, spouse's name, and TIN), items 15-16 (the prior document the passport issuance is based on, and any additional supporting identity documents, each a single compound blank), item 17 (payment receipt or fee-exemption document reference), item 18 (applicant's own signature), and items 19-20 (the receiving clerk's own office-only acceptance code/signature block) — followed by a reverse side (\"тарафи дигари варақа\") with items 21-34 — items 21-22 (father's and mother's full names), items 23-25 (three consecutively numbered blanks captioned with the identical residence-address label), item 26 (a unified national identification number), item 27 (an office-only service-notes line), items 28-31 (the applicant's own previous passport's number, validity period, issuing authority, and issue date, for replacement/renewal filers), and items 32-34 (the applicant's own second signature acknowledging responsibility for false information, followed by two further office-only employee/head signature blocks).\n\n**Shakli 1/1** (\"ба Дастурамал\" — appendix to the instruction, the application's own continuation sheet): a system-assigned application sequence number (\"Рақами ариза\", printed as a 12-digit placeholder, `000000000001`) and an unexplained \"Афзалият\" (priority/preference) marker; a \"Маълумотҳои шахсӣ\" (personal data) block restating the applicant's name, nationality, national number, TIN, date of birth, sex, blood group, marital status, place of birth, address/phone, and spouse — all already captured on Shakli 1 itself; a \"Мавҷудияти изи ангуштҳо\" (fingerprint availability) section with left-hand/right-hand indicators; an eight-question \"Саволнома\" (Questionnaire) of Yes/No (Ҳа/Не) eligibility questions; and a \"Пардохт\" (Payment) block (payment date, receipt number, discount type/percentage, amount) closed by a final applicant signature line.\n\n## Scope: excluded content\n\n- **Shakli 1/1's own restatement of the applicant's personal data** (name, nationality, national number, TIN, date of birth, sex, blood group, marital status, place of birth, address, spouse) is excluded in its entirety: every one of these facts is already modelled once from Shakli 1 itself (items 1-14, 26), and the continuation sheet's own layout and heading (\"Маълумотҳои шахсӣ\") read as a system-populated summary printed from the same underlying record, not a second independent data-collection point.\n- **`Рақами ариза` (application sequence number) and `Афзалият`** are excluded: the printed `000000000001` placeholder is plainly a system-assigned sequence counter, not applicant-supplied data, and `Афзалият` (\"priority\"/\"preference\") carries no explanatory text anywhere in this document — this Zamimaho bundle contains only the appendix forms themselves, not the instruction (\"Дастурамал\") document each appendix is captioned as belonging to, so this field's meaning cannot be confirmed from the available source and is disclosed as excluded rather than guessed.\n- **The fingerprint-availability indicators** (left hand/right hand) are excluded as an office-completed confirmation that biometric capture occurred during in-person enrollment, not applicant-supplied data — the same class of exclusion this registry draws around other office-only confirmation marks (e.g. `tj/andoz/simplified-tax-declaration`'s tax-inspector acceptance block).\n- **The Payment block** (payment date, receipt number, discount type/percentage, amount) is excluded as office/cashier-supplied transaction data, mirroring this registry's standard treatment of payment-processing blocks; the applicant-facing obligation to present a receipt or fee-exemption document is instead captured as the `paymentReceiptOrFeeExemptionDocument` document requirement (Shakli 1, item 17).\n- **Items 19-20, 27, and 33-34** (the receiving clerk's acceptance code/signature/date, the service-notes line, and the processing employee's and department head's own signature blocks) are excluded as office-only, the same class of exclusion as this registry's `tj/andoz/simplified-tax-declaration` tax-inspector block.\n- **The applicant's own signature marks themselves** (Shakli 1 items 18 and 32) are not separately modelled as data — captured instead by the `signedApplicationDeclaration` document's own `required: true`, consistent with this registry's established signature-block convention (e.g. `tj/andoz/simplified-tax-declaration`, `bo/sin/formulario-200-iva`).\n\n## Disclosed findings and interpretation choices\n\n1. **Items 23, 24, and 25 print the identical residence-address caption three times in a row** (\"суроғаи ҷойи зист ва ё суроғаи ҷойи будубош: вилоят, ноҳия, нуқтаи аҳолинишин, кӯча, хона, бино, ҳуҷра\" — \"address of residence or address of actual stay: region, district, locality, street, house, building, room\"), each with its own full-width blank line and its own item number, with no distinguishing text anywhere in the source explaining why three numbered blanks share one caption. Rather than invent a semantic distinction the form itself does not state (e.g. guessing these are three different addresses), this schema models them as three sequential bounded slots — `residentialAddressLine1` (required, since a residence address cannot be entirely absent), `residentialAddressLine2` and `residentialAddressLine3` (both optional continuation lines) — the same bounded-slot treatment this registry has applied to other forms with repeated, undifferentiated numbered blanks.\n2. **`sex` (item 4, \"ҷинс\") is modelled as a free-text `string`, not an `enum`**, because — unlike `maritalStatus` (item 12), whose blank is captioned with its own two printed options (\"оиладор/муҷаррад\") — this item's blank carries no printed option list at all, only the bare category label. Modelling it as a string avoids asserting a closed value set the source itself does not print.\n3. **`maritalStatus` (item 12) is modelled as a 2-value enum** (`married`, `single`), mapped from the form's own printed \"оиладор/муҷаррад\" (married/unmarried) option pair directly beneath the blank.\n4. **`spouseFullName` (item 13) is modelled `requiredWhen` `maritalStatus` equals `married`, static `required: false`**, since a single applicant has no spouse to name and the form provides no other gating mechanism for this field.\n5. **Items 15 and 16 (\"шиноснома дода шудааст дар асоси\" and \"Ҳуҷҷатҳои иловагӣ\") are each modelled as a single compound `string` field**, not decomposed into separate document-name/series/number/date/authority fields, because each is printed as exactly one continuous underscore blank beneath a caption listing several sub-facts to write into that one line — unlike the later previous-passport block (items 28-31), where each of those same sub-facts (number, validity period, issuing authority, issue date) gets its own separately numbered blank and is modelled as a separate field accordingly.\n6. **`nationalIdNumber` (item 26, \"рақами ягонаи миллии\") is modelled `required: true`**, as Tajikistan's own unified national identification number, a distinct concept from both `tin` (item 14, the tax identifier) and the passport's own future serial number (not yet assigned at application time, hence not itself a field on this form).\n7. **Items 28-31 (the applicant's own previous passport details) are all modelled `required: false`**, since a first-time applicant (per the very process this unified form covers) has no previous passport to report; the form gives no separate first-time/renewal/replacement selector field distinguishing these cases, so requiredness cannot be conditioned on an applicant-supplied value and is instead left optional across the board, consistent with this registry's treatment of renewal-only blocks on other first-time/renewal/replacement combined forms (e.g. `zw/rg/passport-application`'s own lost/stolen-particulars section).\n8. **The eight Саволнома (Questionnaire) items are each modelled as a `boolean` field, `required: true`.** Two pairs read as near-duplicates of each other at first glance (`q1SecretInfoAuthorizationFormsOneAndTwo` and `q7PriorSecretInfoAuthorization`) but are grammatically distinct in the source — q1 uses the present-perfect \"дароварда шудааст\" (\"has been formalized\", i.e. a currently-standing authorization) while q7 uses the past-perfect \"дароварда шуда буд\" (\"had been formalized\", i.e. an authorization that existed at some past point, phrasing consistent with asking about a since-lapsed or revoked clearance) — both are modelled as distinct fields rather than collapsed into one, since the form itself prints them as two separate questions.\n9. **No field on this form carries a visible mandatory-field asterisk or equivalent marker** anywhere in the extracted text. Per this registry's own precedent for un-asterisked sources (e.g. `tj/andoz/simplified-tax-declaration`, `bo/sin/formulario-200-iva`), fields are modelled `required: true` only where the form's own layout and this application's basic purpose make the fact essential to processing any application at all (the applicant's own current name, sex, date/place of birth, marital status, parents' names, current residence, national ID number, the passport-issuance-basis document, and all eight questionnaire answers), and `required: false` elsewhere (prior names, blood group, TIN, additional documents, previous-passport particulars, and the two address continuation lines).\n\n## Conformance\n\n3 valid mock scenarios — `valid-first-time-single-applicant` (a never-married first-time applicant with no previous passport and no prior-name history), `valid-renewal-married-applicant-with-spouse` (a married renewal applicant reporting a previous passport and a spouse), and `valid-name-change-divorced-applicant` (a divorced applicant reporting a prior (maiden) name and additional supporting documents) — plus 8 mutation-control fixtures (one missing statically-required field from each of `surname`, `givenName`, `sex`, `dateOfBirth`, `maritalStatus`, `fatherFullName`, `residentialAddressLine1`, and `q8OtherCitizenship`) and one unknown-field-rejected fixture, committed under `conformance/tj/vkd/internal-passport-application/1.0.0/`.\n\nAn ephemeral, from-scratch conformance checker (deriving required/requiredWhen rules directly from this schema's own `fields[]`, discarded after use, not committed) ran all fixtures: all 3 valid scenarios at 0 errors, all 8 mutation controls each raising exactly 1 error, and the unknown-field fixture correctly rejected. Validated clean with `node tools/validate.mjs` and `node tools/validate-ajv.mjs`, individually and as part of the full registry run. `registry-index.json` regenerated via `npm run build-index` in `tools/govschema-client/`."
  },
  "maturity": {
    "level": "structural-reference",
    "criteria": {
      "structuralReference": true,
      "verifiedSchema": false,
      "agentReadySchema": false,
      "executionTestedSchema": false
    },
    "method": "maturity-self-assessment-v1",
    "assertedBy": "GovSchema Engineering (Standards Engineer)",
    "assertedAt": "2026-07-23"
  },
  "license": "CC-BY-4.0",
  "steps": [
    {
      "id": "applicant_identity",
      "title": "Applicant's current and prior identity (Shakli 1, items 1-14)",
      "fields": ["surname", "givenName", "patronymic", "sex", "priorSurname", "priorGivenName", "priorPatronymic", "dateOfBirth", "nationality", "placeOfBirth", "bloodGroupRhFactor", "maritalStatus", "spouseFullName", "tin"],
      "next": "prior_documents"
    },
    {
      "id": "prior_documents",
      "title": "Basis and supporting identity documents (Shakli 1, items 15-16)",
      "fields": ["passportIssuanceBasisDocument", "additionalIdentityDocuments"],
      "next": "family_and_residence"
    },
    {
      "id": "family_and_residence",
      "title": "Parents, residence, and national ID number (Shakli 1, reverse side, items 21-26)",
      "fields": ["fatherFullName", "motherFullName", "residentialAddressLine1", "residentialAddressLine2", "residentialAddressLine3", "nationalIdNumber"],
      "next": "previous_passport"
    },
    {
      "id": "previous_passport",
      "title": "Previous passport particulars, for renewal/replacement applicants (Shakli 1, reverse side, items 28-31)",
      "fields": ["previousPassportNumber", "previousPassportValidityPeriod", "previousPassportIssuingAuthority", "previousPassportIssueDate"],
      "next": "questionnaire"
    },
    {
      "id": "questionnaire",
      "title": "Саволнома — Eligibility questionnaire (Shakli 1/1)",
      "fields": ["q1SecretInfoAuthorizationFormsOneAndTwo", "q2AwareOfClassifiedInfo", "q3MilitaryConscriptionCallUp", "q4CriminalRecordOrProsecution", "q5CourtOrderedObligationsCompliance", "q6ContractualObligationsRestrictingForeignTravel", "q7PriorSecretInfoAuthorization", "q8OtherCitizenship"]
    }
  ],
  "fields": [
    {
      "name": "surname",
      "label": "1. Насаб",
      "type": "string",
      "required": true,
      "classification": "pii",
      "description": "Applicant's current surname.",
      "sourceRef": "Shakli 1, Item 1 — \"насаб\""
    },
    {
      "name": "givenName",
      "label": "2. Ном",
      "type": "string",
      "required": true,
      "classification": "pii",
      "description": "Applicant's given name.",
      "sourceRef": "Shakli 1, Item 2 — \"ном\""
    },
    {
      "name": "patronymic",
      "label": "3. Номи падар",
      "type": "string",
      "required": true,
      "classification": "pii",
      "description": "Applicant's patronymic (father's name).",
      "sourceRef": "Shakli 1, Item 3 — \"номи падар\""
    },
    {
      "name": "sex",
      "label": "4. Ҷинс",
      "type": "string",
      "required": true,
      "classification": "pii",
      "description": "Applicant's sex, as printed on the form. The source prints only the bare label with no enumerated option list (see this document's own VERIFICATION.md).",
      "sourceRef": "Shakli 1, Item 4 — \"ҷинс\""
    },
    {
      "name": "priorSurname",
      "label": "5. То бастани ақди никоҳ / насаби қаблӣ",
      "type": "string",
      "required": false,
      "classification": "pii",
      "description": "Applicant's surname before marriage, or otherwise-prior surname, when different from the current surname.",
      "sourceRef": "Shakli 1, Item 5"
    },
    {
      "name": "priorGivenName",
      "label": "6. Номи қаблӣ",
      "type": "string",
      "required": false,
      "classification": "pii",
      "description": "Applicant's prior given name, when different from the current given name.",
      "sourceRef": "Shakli 1, Item 6"
    },
    {
      "name": "priorPatronymic",
      "label": "7. Номи падари қаблӣ",
      "type": "string",
      "required": false,
      "classification": "pii",
      "description": "Applicant's prior patronymic, when different from the current patronymic.",
      "sourceRef": "Shakli 1, Item 7"
    },
    {
      "name": "dateOfBirth",
      "label": "8. Санаи таваллуд",
      "type": "date",
      "required": true,
      "classification": "pii",
      "description": "Applicant's date of birth.",
      "sourceRef": "Shakli 1, Item 8"
    },
    {
      "name": "nationality",
      "label": "9. Миллат",
      "type": "string",
      "required": true,
      "classification": "pii",
      "description": "Applicant's ethnic nationality (e.g. тоҷик — Tajik), distinct from citizenship.",
      "sourceRef": "Shakli 1, Item 9"
    },
    {
      "name": "placeOfBirth",
      "label": "10. Ҷойи таваллуд",
      "type": "string",
      "required": true,
      "classification": "pii",
      "description": "Applicant's place of birth: country, region (вилоят), district (ноҳия), and locality (нуқтаи аҳолинишин), written together in the form's own single blank.",
      "sourceRef": "Shakli 1, Item 10"
    },
    {
      "name": "bloodGroupRhFactor",
      "label": "11. Гурӯҳи хун, резус-фактор",
      "type": "string",
      "required": false,
      "classification": "health",
      "description": "Applicant's blood group and Rh factor (e.g. \"II Rh+\"), when known.",
      "sourceRef": "Shakli 1, Item 11"
    },
    {
      "name": "maritalStatus",
      "label": "12. Вазъи оилавӣ",
      "type": "enum",
      "required": true,
      "description": "Applicant's marital status, per the form's own printed option pair: married (оиладор) or single (муҷаррад).",
      "sourceRef": "Shakli 1, Item 12 — \"оиладор/муҷаррад\"",
      "validation": {
        "enum": ["married", "single"]
      }
    },
    {
      "name": "spouseFullName",
      "label": "13. Ҳамсар",
      "type": "string",
      "required": false,
      "classification": "pii",
      "description": "Spouse's surname, given name, and patronymic, written together in the form's own single blank. Applicable only when the applicant is married.",
      "sourceRef": "Shakli 1, Item 13",
      "requiredWhen": {
        "field": "maritalStatus",
        "equals": "married"
      }
    },
    {
      "name": "tin",
      "label": "14. РМА",
      "type": "string",
      "required": false,
      "classification": "pii",
      "description": "Applicant's Taxpayer Identification Number (РМА / TIN), when the applicant has one.",
      "sourceRef": "Shakli 1, Item 14"
    },
    {
      "name": "passportIssuanceBasisDocument",
      "label": "15. Шиноснома дода шудааст дар асоси",
      "type": "string",
      "required": true,
      "description": "The prior document this passport application is based on — its name, series, number, date issued, and issuing authority, written together in the form's own single blank (e.g. a birth certificate for a first-time applicant, or the applicant's own previous passport for a renewal/replacement).",
      "sourceRef": "Shakli 1, Item 15"
    },
    {
      "name": "additionalIdentityDocuments",
      "label": "16. Ҳуҷҷатҳои иловагӣ",
      "type": "string",
      "required": false,
      "description": "Any additional supporting identity documents presented — name, series, number, date issued, and issuing authority, written together in the form's own single blank.",
      "sourceRef": "Shakli 1, Item 16"
    },
    {
      "name": "fatherFullName",
      "label": "21. Падар",
      "type": "string",
      "required": true,
      "classification": "pii",
      "description": "Father's surname, given name, and patronymic, written together in the form's own single blank.",
      "sourceRef": "Shakli 1, reverse side, Item 21"
    },
    {
      "name": "motherFullName",
      "label": "22. Модар",
      "type": "string",
      "required": true,
      "classification": "pii",
      "description": "Mother's surname, given name, and patronymic, written together in the form's own single blank.",
      "sourceRef": "Shakli 1, reverse side, Item 22"
    },
    {
      "name": "residentialAddressLine1",
      "label": "23. Суроғаи ҷойи зист ва ё суроғаи ҷойи будубош",
      "type": "string",
      "required": true,
      "classification": "pii",
      "description": "Applicant's address of residence or address of actual stay: region (вилоят), district (ноҳия), locality (нуқтаи аҳолинишин), street (кӯча), house (хона), building (бино), and room (ҳуҷра). The form prints three consecutively numbered blanks (items 23-25) sharing this identical caption with no distinguishing text; this schema models them as sequential bounded slots for one address that may not fit on a single line — see this document's own VERIFICATION.md.",
      "sourceRef": "Shakli 1, reverse side, Item 23"
    },
    {
      "name": "residentialAddressLine2",
      "label": "24. Суроғаи ҷойи зист ва ё суроғаи ҷойи будубош (идома)",
      "type": "string",
      "required": false,
      "classification": "pii",
      "description": "Continuation of the residence address, when it does not fit on the first line.",
      "sourceRef": "Shakli 1, reverse side, Item 24"
    },
    {
      "name": "residentialAddressLine3",
      "label": "25. Суроғаи ҷойи зист ва ё суроғаи ҷойи будубош (идома)",
      "type": "string",
      "required": false,
      "classification": "pii",
      "description": "Further continuation of the residence address, when it does not fit on the first two lines.",
      "sourceRef": "Shakli 1, reverse side, Item 25"
    },
    {
      "name": "nationalIdNumber",
      "label": "26. Рақами ягонаи миллӣ",
      "type": "string",
      "required": true,
      "classification": "pii",
      "description": "Applicant's unified national identification number, distinct from both the TIN and the passport's own future serial number.",
      "sourceRef": "Shakli 1, reverse side, Item 26"
    },
    {
      "name": "previousPassportNumber",
      "label": "28. Рақами шиносномаа",
      "type": "string",
      "required": false,
      "classification": "pii",
      "description": "Number of the applicant's previous internal passport, for renewal or replacement applications. Not applicable to a first-time applicant.",
      "sourceRef": "Shakli 1, reverse side, Item 28"
    },
    {
      "name": "previousPassportValidityPeriod",
      "label": "29. Мӯҳлати амал",
      "type": "string",
      "required": false,
      "description": "Validity period of the applicant's previous internal passport. Not applicable to a first-time applicant.",
      "sourceRef": "Shakli 1, reverse side, Item 29"
    },
    {
      "name": "previousPassportIssuingAuthority",
      "label": "30. Мақомоте, ки шиносномаро додааст",
      "type": "string",
      "required": false,
      "description": "Authority that issued the applicant's previous internal passport. Not applicable to a first-time applicant.",
      "sourceRef": "Shakli 1, reverse side, Item 30"
    },
    {
      "name": "previousPassportIssueDate",
      "label": "31. Санаи гирифтани шиноснома",
      "type": "date",
      "required": false,
      "description": "Date the applicant obtained their previous internal passport. Not applicable to a first-time applicant.",
      "sourceRef": "Shakli 1, reverse side, Item 31"
    },
    {
      "name": "q1SecretInfoAuthorizationFormsOneAndTwo",
      "label": "Оё ба Шумо дар давоми фаъолияти корӣ (донишомузӣ, хизмати ҳарбӣ) бо шакли №1 ва №2 ба маълумоти дорои сирри давлатӣ дошта иҷозатнома ба расмият дароварда шудааст?",
      "type": "boolean",
      "required": true,
      "fieldRole": "eligibility",
      "description": "Whether the applicant currently holds a state-secret information access authorization (via Form No. 1 or No. 2) granted during their work, study, or military service.",
      "sourceRef": "Shakli 1/1, Саволнома, Question 1"
    },
    {
      "name": "q2AwareOfClassifiedInfo",
      "label": "Оё Шумо аз маълумоти махфӣ ва махсусан махфӣ, ки сирри давлатиро ташкил медиҳад хабардор ҳастед?",
      "type": "boolean",
      "required": true,
      "fieldRole": "eligibility",
      "description": "Whether the applicant is aware of confidential or top-secret information constituting a state secret.",
      "sourceRef": "Shakli 1/1, Саволнома, Question 2"
    },
    {
      "name": "q3MilitaryConscriptionCallUp",
      "label": "Оё Шумо барои хизмати ҳарбӣ дар сафи қувваҳои мусаллаҳи Ҷумҳурии Тоҷикистон даъват шудаед (барои мардони синну солашон аз 18 то 27)?",
      "type": "boolean",
      "required": true,
      "fieldRole": "eligibility",
      "description": "Whether the applicant (men aged 18 to 27) has been called up for military service in the Armed Forces of the Republic of Tajikistan.",
      "sourceRef": "Shakli 1/1, Саволнома, Question 3"
    },
    {
      "name": "q4CriminalRecordOrProsecution",
      "label": "Оё Шумо барои содир намудани ҷиноят маҳкум шудаед ва ё таҳти таъқиботи ҷиноятӣ дар қаламрави ҷумҳурӣ ё берун аз он қарор доред?",
      "type": "boolean",
      "required": true,
      "fieldRole": "eligibility",
      "description": "Whether the applicant has been convicted of a crime, or is under criminal prosecution, within or outside the republic.",
      "sourceRef": "Shakli 1/1, Саволнома, Question 4"
    },
    {
      "name": "q5CourtOrderedObligationsCompliance",
      "label": "Оё Шумо аз ӯҳдадориҳои муқаррарнамудаи Суд, ки тибки санади қувваи қонунӣ пайдо намудаи он муқаррар шудааст, саркашӣ накарда истодаед?",
      "type": "boolean",
      "required": true,
      "fieldRole": "eligibility",
      "description": "Whether the applicant is not evading court-ordered obligations established by a legally effective judicial act.",
      "sourceRef": "Shakli 1/1, Саволнома, Question 5"
    },
    {
      "name": "q6ContractualObligationsRestrictingForeignTravel",
      "label": "Оё Шумо аз ӯҳдадориҳои шартномавӣ (созишномавӣ) доред, ки барои сафари хориҷиатон монеа шуда тавонад?",
      "type": "boolean",
      "required": true,
      "fieldRole": "eligibility",
      "description": "Whether the applicant has contractual obligations that could impede their foreign travel.",
      "sourceRef": "Shakli 1/1, Саволнома, Question 6"
    },
    {
      "name": "q7PriorSecretInfoAuthorization",
      "label": "Оё ба Шумо дар давоми фаъолияти корӣ (донишомузӣ, хизмати ҳарбӣ) ба маълумоти махфӣ ва махсусан махфӣ, ки сирри давлатиро ташкил медиҳад, иҷозатнома ба расмият дароварда шуда буд?",
      "type": "boolean",
      "required": true,
      "fieldRole": "eligibility",
      "description": "Whether the applicant had previously held (past perfect — a since-lapsed or revoked authorization, distinct from Question 1's currently-standing authorization) a state-secret/classified information access authorization granted during their work, study, or military service.",
      "sourceRef": "Shakli 1/1, Саволнома, Question 7"
    },
    {
      "name": "q8OtherCitizenship",
      "label": "Оё Шумо шаҳрвандии дигар кишварро доред?",
      "type": "boolean",
      "required": true,
      "fieldRole": "eligibility",
      "description": "Whether the applicant holds citizenship of another country.",
      "sourceRef": "Shakli 1/1, Саволнома, Question 8"
    }
  ],
  "documents": [
    {
      "id": "applicantPhoto",
      "label": "Applicant's photograph",
      "category": "supporting-evidence",
      "required": true,
      "sourceRef": "Shakli 1 — \"РАСМ\" photo box; Shakli 1/1 — \"Ҷои сурат\" photo box"
    },
    {
      "id": "paymentReceiptOrFeeExemptionDocument",
      "label": "State fee payment receipt or fee-exemption document",
      "category": "payment",
      "required": true,
      "sourceRef": "Shakli 1, Item 17"
    },
    {
      "id": "signedApplicationDeclaration",
      "label": "Applicant's signed application, acknowledging responsibility for the accuracy of the information provided",
      "category": "attestation",
      "required": true,
      "sourceRef": "Shakli 1, Item 18 and reverse side, Item 32"
    }
  ]
}
